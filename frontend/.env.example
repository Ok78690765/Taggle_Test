# Frontend Environment Configuration Template
# Copy this file to .env.local in the frontend directory and update with your values
# See docs/environment.md for detailed information on each variable

# === API CONFIGURATION ===
# Backend API URL - Critical for frontend-backend communication
# Local Development:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (set in Vercel project environment variables):
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# === APP CONFIGURATION ===
# Application name (displayed in UI)
NEXT_PUBLIC_APP_NAME=Agent Console

# Application version
NEXT_PUBLIC_APP_VERSION=1.0.0

# === ENVIRONMENT ===
# Node environment: development, production
NODE_ENV=development

# === ANALYTICS & MONITORING (Optional) ===
# Google Analytics tracking ID
# NEXT_PUBLIC_ANALYTICS_ID=UA-XXXXXXXXX-X

# Sentry DSN for error tracking and monitoring
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz

# === FEATURE FLAGS (Optional) ===
# Enable/disable specific features
# NEXT_PUBLIC_ENABLE_ADVANCED_ANALYSIS=true
# NEXT_PUBLIC_ENABLE_EXPORT=true

# === DEVELOPMENT ONLY ===
# These should NOT be set in production environment variables
# Only use in .env.local for local development

# Debug mode for logging
# DEBUG=false

# === DEPLOYMENT NOTES ===
# For Vercel deployment:
#   1. Set NEXT_PUBLIC_API_URL to your Railway backend URL in Vercel dashboard
#   2. Use "Environment Variables" in Vercel project settings
#   3. All NEXT_PUBLIC_* variables are exposed to the browser
#   4. Never commit .env.local to git (covered by .gitignore)
#
# For local development:
#   1. Copy this file to .env.local
#   2. Update NEXT_PUBLIC_API_URL to your local backend URL
#   3. .env.local is automatically loaded by Next.js
#   4. .env.local is NOT committed to git
#
# For Docker deployment:
#   1. Build-time environment variables should be passed during docker build
#   2. Or set in docker-compose.yml environment section
#   3. Frontend is a static build - all config at build time
