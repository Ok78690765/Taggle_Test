# Shared Environment Configuration Template
# For local development and Docker Compose orchestration
# Copy this file to .env in the root directory and update with your values
# See docs/environment.md for detailed information on each variable

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Application name
APP_NAME=Agent Console

# Deployment environment: development, staging, production
ENVIRONMENT=development

# Enable debug mode (should be false in production)
DEBUG=false

# ============================================================================
# BACKEND CONFIGURATION (FastAPI)
# ============================================================================

# Backend server host and port
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Full backend URL (used by frontend for API calls)
BACKEND_URL=http://localhost:8000

# Backend database configuration
# SQLite for local development:
DATABASE_URL=sqlite:///./test.db

# PostgreSQL example (if using PostgreSQL instead):
# DATABASE_URL=postgresql://user:password@localhost:5432/myapp

# Backend secret key for session/token signing (change in production!)
SECRET_KEY=your-secret-key-here-change-in-production

# CORS origins allowed by backend (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Backend logging level
LOG_LEVEL=INFO

# Backend API key (if applicable)
API_KEY=your-api-key-here

# ============================================================================
# FRONTEND CONFIGURATION (Next.js)
# ============================================================================

# Frontend server host and port
FRONTEND_HOST=localhost
FRONTEND_PORT=3000

# Full frontend URL
FRONTEND_URL=http://localhost:3000

# CRITICAL: Frontend's connection to backend (must match BACKEND_URL)
# This is the URL the browser uses to reach the backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Frontend app configuration
NEXT_PUBLIC_APP_NAME=Agent Console
NEXT_PUBLIC_APP_VERSION=1.0.0

# Frontend Node environment
NODE_ENV=development

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Default is SQLite for local development
# DATABASE_URL is defined above under BACKEND CONFIGURATION

# For PostgreSQL:
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=myapp

# ============================================================================
# SECURITY & SECRETS
# ============================================================================

# API Keys and sensitive credentials
# These should be environment variables in production platforms
# Never commit actual secrets to git

# For development/testing only:
SECRET_KEY=your-secret-key-here-change-in-production
API_KEY=your-api-key-here

# ============================================================================
# FEATURE FLAGS
# ============================================================================

# Enable API documentation endpoints
ENABLE_API_DOCS=true

# ============================================================================
# DOCKER & DEPLOYMENT NOTES
# ============================================================================

# For Docker Compose local development:
#   - These values work with docker-compose.yml
#   - Services communicate internally using service names (e.g., backend:8000)
#   - External access uses localhost:PORT
#   - Ensure NEXT_PUBLIC_API_URL matches BACKEND_URL for browser access
#
# For Railway/Heroku deployment:
#   1. Set ENVIRONMENT=production
#   2. Set DEBUG=false
#   3. Use platform-provided DATABASE_URL
#   4. Set SECRET_KEY in platform dashboard (not in .env)
#   5. Set CORS_ORIGINS to your Vercel frontend URL
#   6. Set NEXT_PUBLIC_API_URL to your Railway API URL in Vercel dashboard
#
# For Vercel frontend:
#   - Set NEXT_PUBLIC_API_URL and other NEXT_PUBLIC_* vars in Vercel dashboard
#   - These are injected at build time, not runtime
#   - All NEXT_PUBLIC_* vars are exposed to the browser
#
# For local development:
#   - Copy to .env
#   - .env is gitignored and not committed
#   - .env.local takes precedence in Next.js
