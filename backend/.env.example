# Backend Environment Configuration Template
# Copy this file to .env in the backend directory and update with your values
# See docs/environment.md for detailed information on each variable

# === APPLICATION SETTINGS ===
# Application name and version
APP_NAME=FastAPI Backend
APP_VERSION=0.1.0

# === ENVIRONMENT & DEBUG ===
# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode - set to false in production
DEBUG=false

# === LOGGING ===
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# === SERVER CONFIGURATION ===
# Server host and port
# BACKEND_HOST - Set to 0.0.0.0 for Docker, 127.0.0.1 for local development
BACKEND_HOST=0.0.0.0

# BACKEND_PORT or PORT - Port on which the API server listens
BACKEND_PORT=8000
# Alternative (Railway uses PORT):
# PORT=8000

# === DATABASE ===
# Local Development (SQLite):
DATABASE_URL=sqlite:///./test.db

# PostgreSQL (replace with your credentials):
# DATABASE_URL=postgresql://user:password@localhost:5432/myapp

# Production (Railway, Heroku, etc.):
# DATABASE_URL=postgresql://user:password@hostname:5432/database

# === SECURITY ===
# Secret key for session/token signing (change in production!)
# Generate a strong key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# === CORS CONFIGURATION ===
# Comma-separated list of allowed origins
# Local development: http://localhost:3000,http://localhost:5173
# Production example: https://yourdomain.com,https://app.yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# === API KEYS & INTEGRATIONS ===
# API Key for internal use (if needed)
API_KEY=your-api-key-here-change-in-production

# GitHub token for code analysis (optional)
# GITHUB_TOKEN=your-github-token-here

# === FEATURE FLAGS ===
# Enable/disable API documentation endpoints (/docs, /redoc)
ENABLE_API_DOCS=true

# === DEPLOYMENT NOTES ===
# For Railway deployment:
#   - Set ENVIRONMENT=production
#   - Set DEBUG=false
#   - Use Railway's DATABASE_URL (automatically provided)
#   - Use Railway's SECRET_KEY (set in dashboard)
#   - Set CORS_ORIGINS to your Vercel frontend URL
#   - All secrets should be set in Railway dashboard, not in .env files
#
# For Vercel/Docker local development:
#   - Use the defaults above
#   - Override in docker-compose.yml or .env.local files
#
# For Heroku:
#   - Similar to Railway - use platform's config vars
#   - Use PORT instead of BACKEND_PORT
